import styled, {css} from 'styled-components'

export const LanceJobContainer = styled.div`
    ${() => css`
        display: flex;
        flex-direction: column;
        gap: 20px;
    `}
`

export const LanceJobBox = styled.div``